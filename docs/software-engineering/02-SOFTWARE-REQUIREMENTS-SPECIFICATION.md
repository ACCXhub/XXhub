# 软件需求规格说明

功能：管理目标、文案、计划、日志和备份；测试中心提供只读预检、诊断、夹具与自移除。非功能：127.0.0.1 本地服务、响应式桌面 UI、可重复测试。安全和隐私：不发送模块测试消息，模块数据位于 `data/modules/autody-test-center`，卸载仅删除该目录。验收：测试中心全宽、模态框可见、模块未安装时不加载资源、普通发送仍有登录、身份、会话、编辑器、确认和浏览器锁校验。
