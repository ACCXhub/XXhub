# 安装与更新

下载 `AutoDy-Windows-Portable.zip`，解压后运行 `install.cmd`。安装器创建项目内 `.venv` 和 Chromium；首次启动后再手动登录。更新时退出管理台后覆盖程序文件，不覆盖 `config.yaml`、`messages.txt` 或 `data/`。

测试中心默认未安装。仅从发布页下载并校验 `AutoDy-Test-Center.autody-module.zip` 的 SHA-256 后，在设置页选择官方 ZIP 安装。
